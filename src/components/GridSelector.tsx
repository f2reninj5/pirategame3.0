import React from 'react'
import ChooseQueue from './ChooseQueue'

function GridSelector (): React.JSX.Element {
  return (
    <div>
      <ChooseQueue></ChooseQueue>
    </div>
  )
}

export default GridSelector
